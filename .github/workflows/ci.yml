name: ci
on:
  pull_request:
  push:
    branches: [ main, develop ]
jobs:
  sanity:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "SansaCloth CI sanity check OK"
